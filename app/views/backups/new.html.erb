<div class="max-w-2xl mx-auto">
  <h1 class="text-3xl font-bold text-tamu-gray-800 mb-6">Backup & Restore</h1>

  <div class="grid grid-cols-1 md:grid-cols-2 gap-4">
    <!-- Download Backup Card -->
    <div class="card">
      <div class="card-header">
        <h3 class="text-xl font-semibold">Download Backup</h3>
      </div>
      <div class="card-body">
        <p class="text-tamu-gray-700 mb-4">
          Download all your expenses, categories, groups, and messages as a JSON file.
        </p>
        <%= button_to "Download Backup", 
                      backups_path, 
                      method: :post,
                      class: "btn-maroon w-full",
                      form: { data: { turbo: false } } %>
      </div>
    </div>

    <!-- Restore Backup Card -->
    <div class="card">
      <div class="card-header">
        <h3 class="text-xl font-semibold">Restore Backup</h3>
      </div>
      <div class="card-body">
        <p class="text-tamu-gray-700 mb-4">
          Upload a previously downloaded backup file to restore your data.
        </p>
        
        <%= form_with url: restore_backups_path, 
                      method: :post, 
                      multipart: true,
                      local: true do |f| %>
          <div class="mb-4">
            <%= f.label :backup_file, "Select Backup File", class: "form-label" %>
            <%= f.file_field :backup_file, 
                             accept: ".json",
                             required: true,
                             class: "form-control" %>
          </div>
          
          <div class="bg-yellow-50 border border-yellow-200 rounded p-3 mb-4">
            <p class="text-sm text-yellow-800">
              <strong>Warning:</strong> This will create new entries from the backup. 
              Existing data will not be deleted.
            </p>
          </div>
          
          <%= f.submit "Restore Backup", 
                       class: "btn-primary w-full",
                       data: { confirm: "Are you sure you want to restore this backup? This will add entries from the backup file." } %>
        <% end %>
      </div>
    </div>
  </div>

  <div class="card mt-4">
    <div class="card-body">
      <h4 class="text-lg font-semibold mb-2">What's included in the backup?</h4>
      <ul class="list-disc list-inside text-tamu-gray-700 space-y-1">
        <li>All your personal expenses</li>
        <li>Categories you've used</li>
        <li>Groups you're a member of</li>
        <li>Your conversations and messages</li>
      </ul>
      
      <h4 class="text-lg font-semibold mt-4 mb-2">Important Notes:</h4>
      <ul class="list-disc list-inside text-tamu-gray-700 space-y-1">
        <li>Backup files are in JSON format</li>
        <li>Keep your backup files secure as they contain your financial data</li>
        <li>When restoring, you may need to rejoin groups manually if you're not already a member</li>
        <li>Restore will not create duplicate entries if the exact data already exists</li>
      </ul>
    </div>
  </div>

  <div class="mt-6">
    <%= link_to "← Back to Dashboard", dashboard_path, class: "text-tamu-maroon hover:underline" %>
  </div>
</div>